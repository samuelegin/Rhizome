function DemoModeBanner() {
  return (
    <div className="demo-mode-banner">
      <i className="bi bi-info-circle" />
      Demo data — no live Envio indexer or deployed contracts yet. What you see here is the shape the real
      product will show.
    </div>
  )
}

export default DemoModeBanner

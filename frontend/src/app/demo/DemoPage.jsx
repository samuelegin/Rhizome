import ComingNext from '../../components/ui/ComingNext'

function DemoPage() {
  return (
    <ComingNext
      title="Demo Environment"
      body="Alice votes alongside Bob in Rio governance, Envio detects the shared participation, a Verified Connection appears, and Alice's membership qualification updates — live, in this view. Built last, after the core flow."
    />
  )
}

export default DemoPage
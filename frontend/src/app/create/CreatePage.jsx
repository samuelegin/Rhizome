import ComingNext from '../../components/ui/ComingNext'

function CreatePage() {
  return (
    <ComingNext
      title="Create Rhizome"
      body="Name, relationship signal, freshness period, minimum connections, and up to 3 bootstrap members — a simple form, no rule builder."
    />
  )
}

export default CreatePage

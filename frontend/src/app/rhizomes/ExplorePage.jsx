import ComingNext from '../../components/ui/ComingNext'

function ExplorePage() {
  return (
    <ComingNext
      title="Explore"
      body="A directory of Rhizomes — name, signal, membership requirement, and your current status for each. Coming right after the core network + membership flow."
    />
  )
}

export default ExplorePage
